<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.SleepDebtTracker">
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SleepHoursActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".LastNightActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".ReganDayActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".AlarmActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".AboutActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".AlarmRingingActivity"
            android:showOnLockScreen="true"
            android:screenOrientation="portrait"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            android:noHistory="true"></activity>

        <!--<service android:enabled="true" android:name=".AlarmService"></service>-->

        <!--<receiver android:name=".AlarmBroadCast" >-->
            <!--<intent-filter>-->
                <!--<action android:name="com.android.techtrainner" />-->
                <!--<action android:name="android.intent.action.BOOT_COMPLETED" />-->
            <!--</intent-filter>-->
        <!--</receiver>-->
        <receiver android:process=":remote" android:name=".AlarmBroadCast"></receiver>
    </application>

</manifest>